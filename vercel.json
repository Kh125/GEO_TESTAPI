{
    "version": 2,
    "buildCommand": "npm run build",
    "build": {
            "src": "./server.js",
            "use": "@vercel/node"
    },
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}